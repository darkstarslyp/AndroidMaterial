<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="ActivityAnim" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/activity_enter_from_bottom</item>
        <item name="android:activityCloseExitAnimation">@anim/activity_exit_to_bottom</item>
        <item name="android:activityOpenExitAnimation">@anim/activty_stay</item>
        <item name="android:activityCloseEnterAnimation">@anim/activty_stay</item>
    </style>

    <style name="ActivityAnimTheme" parent="AppTheme">
         <item name="android:windowAnimationStyle">@style/ActivityAnim</item>
    </style>s


</resources>
